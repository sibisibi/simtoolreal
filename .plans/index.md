# .plans/, session index

<!-- Agent appends a line whenever a new session dir is created under .plans/. Format: `- NNN-name — <one-line description>`. Description is updated from the session's wiki.md on `distill plan`. -->

- 001-template-only — structural template, do not delete
<!-- removed, 002-setup-workflow belongs to the template repo, not this one -->
<!-- - 002-setup-workflow — extract project-agnostic vault, rewrite google-drive.md (MCP, structural sharing), wire first Drive folder (+ e2e smoke), build .pages/ publish surface to the final one-tree model (private repo, public Pages, slides.html+noindex, live), audit the kait chat and rewrite vault/servers/kait/ to the post-2026-05-09 state, build the Notion template hub and decouple readai2notion, finalize the coding style, move the weekly deck to native Google Slides, make the per-experiment .pages Drive-first and slides.html-only with a weekly-first site (drive-vis skill, no html vis) -->
- 002-real-world-deploy — contract from a live env, ROS2 stack with a C++ joint impedance controller matching the trained actuator, FoundationPose perception, sim rehearsal, then the robot session
