# .goals/, session index

<!-- Agent appends a line whenever a new session dir is created under .goals/. Format: `- NNN-name — <one-line description>`. The description is updated from the session's wiki.md when the user runs `distill goals`. -->

- 001-template-only — structural template, do not delete
- 002-real-world-deploy — deploy the sim-trained SimToolReal checkpoint on the lab FR3 plus XHand with one D435i, infrastructure first, ROS2 stack matching the sim actuator exactly
